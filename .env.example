# Environment Configuration for Heard App

# Option 1: Direct Database Connection (Quick Setup)
# Get this from your Replit Secrets tab
# DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: API Proxy Configuration (Recommended)
# Your Replit app URL (e.g., https://your-app.repl.co)
VITE_API_URL=https://your-heard-app.repl.co

# Option 3: Local Development with Production API
# Set this to use production API while developing locally
VITE_USE_PRODUCTION_API=true

# Server Configuration
PORT=5000
NODE_ENV=development

# Admin Configuration (optional - for local testing)
ADMIN_PASSWORD=your_admin_password_here